const Seane3 = (char1, char2, char3, char4, char5) => {
  const str = `${char1}, ${char2}, and ${char3} arrive at the grand palace of Mahishmati. 
${char4} welcomes them with royal grace. Later, ${char4} asks ${char2} to marry ${char5}, 
but ${char2} refuses and chooses ${char1}, following her heart over power.`;
  return str;
};

export default Seane3;
