const Seane4 = (char1, char2) => {
  const str = `In the final twist, ${char2} kills ${char1} — not out of hatred, but loyalty and duty. 
The kingdom mourns, and a legend is born.`;
  return str;
};

export default Seane4;
